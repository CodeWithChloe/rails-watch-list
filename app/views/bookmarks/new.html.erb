<h2>New bookmark</h2>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "Create a movie", class: "btn btn-primary" %>
<% end %>


<%= link_to "Back to my list", list_path(@list), class: "btn btn-secondary" %>
